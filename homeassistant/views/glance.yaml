path: view_glance
title: Glance
cards:
  - type: custom:auto-entities
    card:
      type: glance
      columns: 3
      title: Radon
    filter:
      include:
        - entity_id: "sensor.airthings_*_radon_short_term_average"
    sort:
      method: name
  - type: custom:auto-entities
    card:
      type: glance
      columns: 3
      title: VOC
    filter:
      include:
        - entity_id: "sensor.airthings_*_voc"
    sort:
      method: name
  - type: custom:auto-entities
    card:
      type: glance
      columns: 3
      title: CO2
    filter:
      include:
        - entity_id: "sensor.airthings_*_co2"
    sort:
      method: name
  - type: custom:auto-entities
    card:
      type: glance
      columns: 3
      title: Temperature
    filter:
      include:
        - entity_id: "sensor.airthings_*_temperature"
    sort:
      method: name
  - type: custom:auto-entities
    card:
      type: glance
      columns: 3
      title: Humidity
    filter:
      include:
        - entity_id: "sensor.airthings_*_humidity"
    sort:
      method: name
  - type: custom:auto-entities
    card:
      type: glance
      columns: 3
      title: Rssi
    filter:
      include:
        - entity_id: "sensor.airthings_*_rssi"
    sort:
      method: name
  - type: custom:auto-entities
    card:
      type: glance
      columns: 3
      title: Pressure
    filter:
      include:
        - entity_id: "sensor.airthings_*_pressure"
    sort:
      method: name
  - type: custom:auto-entities
    card:
      type: glance
      columns: 3
      title: Light level
    filter:
      include:
        - entity_id: "sensor.airthings_*_light_level"
    sort:
      method: name
  - type: custom:auto-entities
    card:
      type: glance
      columns: 3
      title: Virus risk
    filter:
      include:
        - entity_id: "sensor.airthings_*_virus_risk_indicator"
    sort:
      method: name
  - type: custom:auto-entities
    card:
      type: glance
      columns: 3
      title: Mold risk
    filter:
      include:
        - entity_id: "sensor.airthings_*_mold_risk_indicator"
    sort:
      method: name
  - type: custom:auto-entities
    card:
      type: glance
      columns: 3
      title: RSSI
    filter:
      include:
        - entity_id: "sensor.airthings_*_rssi"
    sort:
      method: name
  - type: custom:auto-entities
    card:
      type: glance
      columns: 3
      title: Battery
    filter:
      include:
        - entity_id: "sensor.airthings_*_battery"
    sort:
      method: name
  - type: custom:auto-entities
    card:
      type: glance
      columns: 3
      title: Relay device
    filter:
      include:
        - entity_id: "sensor.airthings_*_relay_device"
    sort:
      method: name
